x = input().strip()
n = int(input())
s = [input().strip() for _ in range(n)]

order = {ch: i for i, ch in enumerate(x)}

def key_fn(w):
	return [order[ch] for ch in w]

s.sort(key=key_fn)

print("\n".join(s))
